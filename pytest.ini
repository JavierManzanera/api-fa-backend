[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    integration: requires a reachable PostgreSQL test database (see docker-compose.test.yml)
    flaky: timing/concurrency-sensitive; not part of the standard CI gate (see tests/README.md)
